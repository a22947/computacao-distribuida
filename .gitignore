# --- Dependências (Node.js) ---
# Ignora node_modules em qualquer pasta (raiz, backend ou frontend)
node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# --- Variáveis de Ambiente (SEGURANÇA CRÍTICA) ---
# Nunca envie chaves de API, passwords de DB ou secrets do JWT para o GitHub
.env
.env.local
.env.development.local
.env.test.local
.env.production.local
# Exceção: Pode manter um ficheiro de exemplo (sem dados reais)
!.env.example

# --- Frontend Builds (React/Vite) ---
# Ignora as pastas de build de produção
dist/
build/
frontend/dist/
frontend/build/

# --- Backend ---
# Ignora logs gerados pelo servidor e pastas de cobertura de testes
logs/
*.log
coverage/
.nyc_output/

# --- Docker ---
# Ignora overrides locais do docker-compose (se usar)
docker-compose.override.yml
# Ignora volumes locais se estiverem mapeados dentro do projeto
mongo-data/
redis-data/

# --- IDEs e Editores de Texto ---
.vscode/
.idea/
*.swp
*.swo
.DS_Store
Thumbs.db

# --- Testes e Reports ---
report.[0-9]*.[0-9]*.[0-9]*.[0-9]*.json
